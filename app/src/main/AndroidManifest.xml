<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.recode_final_project">
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Recode_Final_Project">
        <activity
            android:name=".views.department.DepartmentUpdateActivity"
            android:exported="true" />
        <activity
            android:name=".views.department.CreateDepartmentActivity"
            android:exported="true" />
        <activity
            android:name=".item_list.Item_List"
            android:exported="true" />
        <activity
            android:name=".views.department.DepartmentActivity"
            android:exported="true" />
        <activity
            android:name=".views.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".views.MainActivity"
            android:exported="true"
            android:label="[RE]CODE V" />
    </application>

</manifest>